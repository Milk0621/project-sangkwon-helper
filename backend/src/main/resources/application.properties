spring.application.name=sangkwon-helper

# MyBatis 설정
mybatis.mapper-locations=classpath:com/sangkwon/backend/domain/**/mapper/*.xml
mybatis.type-aliases-package=com.sangkwon.backend.domain
mybatis.configuration.map-underscore-to-camel-case=true

# JPA 설정
spring.jpa.hiberante.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hiberante.format_sql=true
spring.jpa.open-in-view=false

# DataSource 공통 설정
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=${DB_URL}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JWT
spring.config.import=optional:application-secret.properties
jwt.access-token-expiration: 900000
jwt.refresh-token-expiration: 604800000